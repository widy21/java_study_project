package com.hx.hxdemo.json;

import com.alibaba.fastjson.JSONArray;
import com.alibaba.fastjson.JSONObject;
import org.apache.commons.lang3.StringUtils;

/**
 * @program: hx-demo
 * @description: ${description}
 * @author: huaxiao
 * @create: 2020-06-22 10:40
 **/
public class TestFilter {

    public static void main(String[] args) {
//        JSONArray jsonArr = JSONArray.parseArray(enumMappingVO.getEnumValueMapInfo());
//        logger.info("in dbInBlackList(), jsonArr = {}",jsonArr.toJSONString());
//        for (Object o : jsonArr) {
//            JSONObject jsonObject = (JSONObject)o;
//            for (String key : jsonObject.keySet()) {
//                if(StringUtils.equalsIgnoreCase(key.trim(),virtualClusterName.trim())
//                        && jsonObject.getString(key).contains(dbsVo.getName())){
//                    logger.info("in dbInBlackList(), dbs name:{} in black list.",dbsVo.getName());
//                    return true;
//                }
//            }
//        }
    }
}
